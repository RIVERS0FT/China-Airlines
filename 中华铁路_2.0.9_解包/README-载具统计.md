# 中华铁路 APK 载具统计

本次直接读取用户提供的 APK，未安装或执行 APK。包名 `com.zhuole.zhtl.ab`，版本 `2.0.9`，versionCode `252`。

**按 StaticTrain.js 中唯一载具 ID 统计，共 759 款配置。**

| 运输类型 | 配置数量 |
|---|---:|
| 客运型 | 226 |
| 货运型 | 238 |
| 客货混合型 | 295 |
| 合计 | 759 |

类型按客位/货位编码是否大于零判断，不依赖名称后缀。

## 统计口径

- 759 个独立 ID 包含客货变体和活动款，不代表 759 种现实机车，也不代表这些配置当前全部可购买或已开放。
- 原始分组字段 col_3 有 303 个不同值；不能直接当作准确的基础车系数量。例如分组 303 同时包含 EMDE8 和 KihaE200。
- 名称去重为 757 个。存在两对名称重复而 ID 不同的记录：神24电力机车客运型（904、906），台铁emu3000城际列车客运型（907、909）；不能按名称删除它们。
- 载具表引用 306 个不同机头图片文件名。APK 内实际有 294 张机头 PNG，其中 4 张未被本表引用；另有 16 个引用文件不在 APK 中，详见统计结果.json。图片数量不能作为载具款数。

## 文件入口

- `原始解包/`：APK 全部 2627 个文件，保留包内目录结构；载具原表为 `原始解包/assets/static_data/StaticTrain.js`。
- `载具清单.csv` / `载具清单.json`：759 款的 ID、名称、运输类型、分组及图片路径。CSV 使用 UTF-8 BOM，便于 Excel 打开。
- `载具分组.csv` / `载具分组.json`：按原始字段分组，保留上述分组局限。
- `配置表/`：全部 273 张静态配置与文案数组的 JSON、CSV 导出。未知字段保留 col_0 起的原列号。
- `统计结果.json`、`配置表索引.json`、`文件清单及哈希.json`：统计和校验依据。
- `extract_and_count.py`：本次提取脚本；重新运行需使用新的目标文件夹，避免覆盖已有解包数据。

APK SHA-256：`a622d164a8b09e5e5d1442fdd1072a44a04b09c4572e5dae028a6ce14855f864`。

已核验完整 ZIP 读取、逐文件大小与哈希、全部配置数组导出一致性、载具 ID 唯一性，以及 CSV/JSON 条目一致性。所有解包内容仅存放在本独立文件夹，未加入中华航空代码仓库。
