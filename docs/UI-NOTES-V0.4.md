# v0.4 引导反馈补充

引导开始、跳过、完成已有引导条/状态变化反馈；操作仍写日志和存档，但不再同时发浮动成功提示，以免遮挡正在教学的客货卡。正常装载、购买反馈不变；引导失败仍显示保存或并发错误。

短横屏开启引导时，卡片内部缩小插图并调整网格行高，保留完整的目的地、数量、报酬与操作/禁止原因文字，不用裁切底部文字换取布局空间。浏览器验收检查操作标签在卡片边界和视口以内。

验证844×390引导开始/跳过无重复浮层，实际点击客货卡后能保存并刷新恢复；不把截图当作实际手机兼容性认证。
