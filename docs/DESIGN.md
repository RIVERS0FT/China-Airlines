# 中华航空 · v0.5 基础能量预算

本轮从界面补齐转向实际运营约束：按已读取的2016年攻略记录“飞行每分钟消耗1点”的比例，新增能量预留、不足停止与地勤补能。并未完成原作全部数值复刻。

## 设计冲突与继承

旧版只检查运营资金、航程和机场等级，可以在有供给时一直自动飞行。本轮明确替换这一无限连续运行规则；除 [ENERGY.md](ENERGY.md) 的能量条款外，完整沿用 [ARCHIVE-V0.4-ROUTES.md](ARCHIVE-V0.4-ROUTES.md) 及其引用的人员、机场浏览、航班运行和经济规则。不能把界面禁用当作核心已实施限制。

默认240点容量取自旧攻略的一个示例，并非确认所有原版车型都是240点。120秒免费地勤补满、按秒预留和取消规则是航空化暂定设计；没有查明原版恢复时点、费用及不足一分钟的舍入，必须在界面和数值档案中明确，不擅称等价。暂不添加未经核实的原版付费货币。

## 运行与兼容

包版本0.5.0，存档格式5，IndexedDB结构仍1；依赖版本不变，lockfile只更新根包版本。每架飞机有独立可用能量、当前航段预留量和补能截止时刻。唯一写入者仍为src/core；UI只显示数据并发送命令，不用动画或计时器回调扣能。

手动、自动、多段全部经过同一depart入口检查并预留。余额不足时当段不能起飞或扣钱；已有在途航班正常抵达。后续计划或自动值勤遇到不足，在实际机场停止，保留已装载订单。补能需完成周转、停止值勤和取消后续计划；完成前不增加能量，取消也不补。补能不是飞行，不产生运输收入、不消耗或清空客货。

冻结v4校验器和catalog-v4。旧版v1—v4先严格验证再迁移到5；旧资金、订单、改装、时刻和已锁定航班不重算，每架旧机赋满初始预算，当前旧航班豁免追扣。只有今后起飞才预留。v5读档不能重复赠能，非法或缺少字段拒绝且不覆盖有效进度。

## 验收与交付

验证整秒耗能、精确余额边界、命令原子性、预览只读、空飞与改装速度、自动缺能停止、多段中转保留、补能时点/取消/刷新/离线、旧存档兼容及损坏字段。保留所有旧测试的业务断言，只调整当前存档版本和新增字段的迁移投影。新增桌面、844×390、667×375补能操作与资金不变验证。

所有合并仍必须通过类型检查、单元、根/子路径构建和零重试浏览器测试，查看截图、校验源码与ZIP后通过PR squash合并。使用既有Pages流程，发布经过验证的同一产物；没有回执不宣称上线。不得覆盖并行工作、修改仓库可见性、购买服务或上传密钥。真实设备与像素级原作验收仍未完成。

## 保留并行合入的城市选路规则

主分支abc4d17已合入按城市点击顺序规划路线的规则，本轮完整保留，不恢复单段/多段切换或航线建设费。城市点击顺序形成最多5段，解锁城市且满足现有等级/航程/状态/资金与新增能量要求即可起飞。routes只是实际运营/兼容记录，预览和失败派航不能写入记录。

v4冻结校验基于该最新路线规则，既接受更早的已购买routes，也接受仅记录首段、尚未记录未来航段的合法v4计划；迁移不加建未来航线，不补扣建设费或旧航班能量。v5重新起飞才预留能量。新能量E2E使用统一城市选择，不依赖已移除的模式或开通按钮。

## 独立发航界面（取代常驻城市工具与全局导航）

本轮精简布局以用户提供的发车截图与已确认方案为基准，详细规则见 [ROUTE-DISPATCH-REFERENCE.md](ROUTE-DISPATCH-REFERENCE.md)。此前常驻资源栏、主导航、城市选择/详情/自动往返/能量工具条的条款被替换；经济与存档约束全部继承，不做数值变更。

默认只显示目的地、预计时间、净利润和成本四项读数；地图直接延伸到页面底部。城市选择由顶部目的地按需打开，路线/能量/自动往返由“查看路线”按需打开。左下缩放与路线后退/撤销/取消/查看，右下唯一“检票起飞”；当飞机在途，返回按钮明确叫“返回航班”。错误和存档冲突保留，正常通知不占发航地图。

取消返回进入前的实际飞机或固定机场，不瞬移、不卸货、不撤销已保存的解锁，也不取消在途航班。地图保持挂载，弹窗不重置草稿和相机。隐藏其他飞机只改变渲染；航程圈围绕末站提示但所有可飞判断仍由核心执行。

三种视口的地图高度至少为85%，主要点击区不小于44像素。回归测试改用新的真实入口，经营/存档原断言保留。包0.5.0、存档5、核心/运行控制器/持久化/依赖不改。通过截图与全量测试后按既有约定打包、squash合并、部署。

## 乘客目的地地图标注

规划路线时，发航地图读取当前飞机的已装载订单，并仅对 `kind === passengers` 且 `location` 为当前飞机 ID 的订单按目的机场汇总。在对应机场旁显示高对比“乘客 N”徽标，N 为该目的地机上旅客人数；货物、其他飞机订单和仍在候机大厅的订单不参与标注。

该标注是纯 UI 只读投影，不修改订单、草稿、航程判断、派航条件或存档，也不新增后台状态。装卸或交付造成机上乘客变化后标注随最新快照更新；当前飞机没有已装载乘客时不显示任何乘客目的地徽标。

## 中华铁路参考界面的机场场景层级

机场装载页继续使用现有原创飞机、候机旅客/货物矢量、机场背景和现有功能，不复制参考游戏的角色、列车、图标、商标、任务文案或付费货币。参考图只用于提炼场景式经营界面的信息层级：顶部资源计数器、中央载具主场景、机场/载具浮动站牌、下部候运客货区、底部功能坞和高优先级发航按钮。

默认机场页将机场信息条改为覆盖在主场景上方的浮动站牌，不再额外占用一整行场景高度；当前机场、飞机状态、旅客/货物/能量仍显示原有真实数据。顶部运营资金、机队和完成航班保持原字段和原入口，但视觉上改为图标徽章加计数器，不新增可购买资源或无效“+”按钮。主飞机继续是最大视觉主体，场景与候运区之间使用原创停机坪/安全线语言形成连续空间。

候运订单仍是现有真实订单并保持卡片点击语义，但视觉弱化管理后台卡片边框，强化目的地牌、人物/货物和奖励读数，使其更像停机坪上的待运对象。底部六个既有功能入口继续保留；“选择航线起飞”仍发送同一进入规划操作，但从功能坞右侧抬升为场景级主按钮，以建立与参考界面“检票发车”相同的操作优先级，不改变可用条件或业务含义。

桌面和横屏手机都必须保持主场景、客货区、功能坞和发航入口同时可达，主要交互区域不小于44×44，页面不得产生横向溢出。844×390 和 667×375 下允许缩短辅助文字与压缩视觉间距，但不得隐藏资金、机队、完成航班、机场、飞机容量/能量、候运订单或发航入口等核心状态。航线地图的独立精简布局不继承本节机场页装饰覆盖规则。
