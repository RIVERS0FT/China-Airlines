# 比例视口与 UI 缩放 · 验证记录

基线：`RIVERS0FT/China-Airlines`，主分支提交 `302f47aa4df17f7c862a855823372d6020dc9543`。

## 实现范围

统一900短边比例画布，修复地图和列表坐标以及所有原生弹窗缩放；新增75%–150%的显示偏好（5%档位、默认100%、恢复默认、本浏览器记忆、存储失败降级）。不修改核心、运行控制器、经济、存档格式及依赖。

## 验证边界

本地环境无法解析npm服务域名，没有完整项目依赖；本地静态/语法检查不能替代项目类型检查、Vitest、生产构建及Playwright。

新增测试随功能分支提交至既有GitHub Actions，必须通过根路径与Pages子路径两轮完整验证，审阅截图后方可squash合并。最终CI结果及部署回执在PR中记录。此前未接入分支的Git tree不代表已发布，本轮以实际分支提交和工作流产物为准。

真机iOS/Safari、Android软键盘与刘海安全区域仍待实机验收，不宣称跨平台逐像素相同。
