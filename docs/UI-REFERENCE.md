# v0.4 界面修订 场景界面对照与验收

本轮不是新增经济系统。承接《中华铁路》的大载具、下方客货队列、地图选路与固定主要操作入口，将其转换为航空场景。更新现有PR #4，不能把分支通过当作已合并或已上线。

## 参考证据边界

沿用已记录的车站装载参考：
https://cdn.ourplay.net/xspace/news/1638343026337334.jpg

2016年1月22日图文教程目录：
https://www.3dmgame.com/gl/3544185.html

本轮可以读取教程文字，但原图链接获取失败；不将“本轮像素级复核”标为完成。尚未锁定统一游戏版本，以下为航空化界面验收，不是原版精确尺寸/公式/全页面完成度。参考图片不进入应用或离线缓存，不新增网络素材请求。

## 装载页

保留顶部资源、机场牌、大飞机、两侧换机、下方客货和右下角起飞入口。客货区用连续候运地面、不同旅客组/行李和货运车表达候运；标签仍然是真正的原生按钮，不能仅展示插图代替装载。

队列左右按钮每次滚动约一屏、保留一项上下文。支持触摸横向滚动和聚焦列表后的左右/Home/End；只有列表自身获得键盘焦点才处理这些按键，不拦截卡片或选择器操作。列表无溢出时禁用翻页。切换飞机、机场、客货分类或机上/候运区时回到队列开头；模拟时间推进和单次装载不主动重置滚动。

卡片显示最终目的地、人数/吨位、锁定报酬与装卸状态。飞行、周转、自动值勤、剩余计划、类型不兼容、客舱/货舱不足、候运区满必须明确说明，禁止显示“点击卸下”但实际不可卸。界面解释只读，核心仍然重新验证每条命令。

短横屏使用同一机体的紧凑侧视：缩短垂尾并去除SVG上下留白，不把整架飞机等比缩成小图标；保留翼面、起落架、舱位和清单入口。桌面继续完整侧视，不改变机型或经营数据。

桌面候运区保持完整竖向卡；844×390与667×375采用人物在左、数量/报酬在右的紧凑卡，关键操作/禁止说明至少11px，不以隐藏文字换取高度。引导期间检查状态文字完整位于卡片内。保持竖屏提示，不依赖锁屏。

## 航网页

单段模式预览当前选中的出发—目的地，多段模式只预览已经添加的草稿，不把尚未添加的选择伪装为正式航段。实线表示满足机型/机场条件，虚线表示不可飞；方向箭头、起点0和访问序号说明顺序。返回同一机场时合并显示访问序号，不隐藏后续访问。实线并不等于已付建设费，文本仍明确“须先开通航线”。

预览调用核心legQuote读取当前航程及机场规则，不复制新的一套飞行公式。清空/撤销/切换模式实时更新图形；预览不能扣费、建航线、派航或改存档。飞行中不展示可执行的地面草稿。自动值勤中手动派航按钮禁用并说明须先停止值勤。

地图保留原生目的地与费用明细、可访问路线文字和WebGL降级入口。所有绘制仅为读快照；与飞机真实位置或收益结算无关。城市字号/相机仍遵循MAP-LAYOUT.md。

## 本轮验收

保留全部v0.4用例，增加：草稿单段/多段/回访/无效段和零经营写入、订单禁用原因、桌面/横屏翻页与筛选复位、空列表边界、在途不可卸提示、引导标签几何边界及草稿实际渲染标记。输出1440×900、844×390、667×375截图。完整CI和最终主分支发布结果以Actions回执为准，模拟视口不等于实际iOS/Android认证。

待完成：统一原作版本、原图恢复后逐页尺寸对照、全部机场美术和动效、原作经营节奏校准。本轮不以新增功能数量换算“复刻百分比”。
