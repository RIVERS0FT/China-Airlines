# 机场参考布局与按钮验证（2026-09-12）

在 `feat/airport-art-assets` 分支将 `origin/main` 快进到 `7173d0a`，保留此前全部未提交美术工作。同步时仅暂存 `src/main.tsx`，解决样式导入冲突后恢复；名为 `airport-art-main-before-sync-20260912` 的 stash 仍保留供追溯。没有提交、推送、PR、合并或部署。

本轮沿用27项原创本地位图，透明素材仍为纯绿背景生成后抠图成品。按用户最新要求改成地面客货队列、右下角发航入口，移除单体订单数量胶囊；旧聚合订单实际数量和核心规则不变。新增真实任务牌、已有功能快捷入口及机体载荷显示开关。

## 修复证据

- 同步后的原界面可复现：装载旅客、筛选货物、点击飞机后，机上旅客列表为空；当前清除旧筛选后显示已装载订单。
- 底部“机场装载”原先只切换页面；当前会回到候机列表。
- 3种尺寸逐个点击原8个入口和新4个入口，检查对应对话框出现与关闭。额外检查机体载荷开关、任务牌不挡住开关、无卡片底色和边框、单体数量胶囊不存在。
- 第一轮完整根路径结果92通过、2失败，原因为底部状态隐藏导致机场数量不可见。恢复真实机场数与离线状态后，最终完整根路径94项全部通过、0重试；未删除原业务断言。

## 最终检查

- 独立类型检查通过。
- 15个文件、294项核心/存档单元测试通过。
- 根路径和 `/China-Airlines/` 生产构建通过，PWA分别预缓存44项。
- 根路径完整94项浏览器测试通过、0重试，约2.5分钟。
- Pages子路径完整94项浏览器测试通过、0重试，约2.5分钟。
- `git diff --check`通过；两种构建中的全部27项美术与源成品SHA256一致。

浏览器覆盖1440×900、844×390、667×375、触控装卸、竖屏提示、路线规划、飞机管理、刷新恢复、损坏存档恢复、导入导出、离线启动与全部美术离线解码。已检查桌面和两种手机横屏截图；`artifacts/airport-controls-1440.png`、`airport-controls-844.png`、`airport-controls-667.png` 为最终机场效果。

构建保留既有Pixi渲染包超过500kB提示。没有验证真实手机/iOS Safari；通用机体未逐机型绘制，不宣称逐像素或全功能复刻。交付ZIP以 `-v3.zip` 命名，保留此前版本；源码ZIP包含当前未提交工作区，静态包分别支持根路径和既有Pages子路径。
