# 第二批美术替换验证（2026-09-12）

本次新增18张，累计27个成品、3,198,209字节。12个按钮/状态图标、头像、4个地勤小物件、飞行机体均已进入页面或已有功能面板；地面机体也从第一批备用素材改为实际主场景显示。第一批验证记录中的“客机尚未替换”仅描述当时状态，以本记录和`docs/ART-ASSETS.md`第二批说明为准。

透明素材保留原始生成图、精确绿底中间图与RGBA成品。第一次导航图集和头像生成未遵循绿底，已保留失败原图并通过内置imagegen纠正后再抠图；未用黑底图充当透明素材。脚本增加源图四角色值检查，避免非绿底原图被误认为处理成功。检查26张PNG均具有0—255透明通道，四角透明。运行时27张图片在根路径、Pages子路径构建中与源成品SHA256一致。

验证结果：

- 类型检查通过；15个测试文件、294项核心/存档单元测试通过。
- 根路径与`/China-Airlines/`生产构建通过，PWA各预缓存44项。
- 根路径完整87项浏览器回归通过，0重试。
- 随后根据截图增大手机横屏底部留白，避免轮子与地勤贴住警戒线；最终根路径17项相关流程复查通过，0重试。
- 最终Pages子路径完整87项浏览器回归通过，0重试，约2.4分钟。
- `git diff --check`通过。

浏览器流程覆盖1440×900、844×390、667×375、竖屏提示、装卸、起飞、机库改装、存档设置、刷新恢复、导入导出与离线启动。新增离线用例解码全部27项成品，检查主导航已使用图标、主场景已引用新机体。已检查桌面、两种手机横屏、飞行截图及透明层浅/深底预览；第二批页面截图文件名以`-v2.png`结尾。

一次浏览器启动因直接导入JSON清单需要Node导入属性而失败；改为`readFileSync`解析后重跑成功。失败启动不计入通过次数。构建仍有既有Pixi渲染包大于500kB提示。

未执行PR、提交、合并或部署；未验证真实手机/iOS Safari、系统最大字号或新增深色主题。保持原布局和动态状态，通用机体未逐机型区分，商店分类示意和简单确认/旋转符号仍为既有SVG，不宣称整套界面已逐像素完成参考图。第二批ZIP以`-v2.zip`命名，保留第一批交付；包内源码是当前未提交工作区。
