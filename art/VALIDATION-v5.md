# 机场客货场景与发布验收（2026-09-12）

本轮累计保留28项原创本地美术：机场、飞机、角色、货物、图标、地勤及新生成的客货地面。新增背景原图和提示词保存于 `art/source/apron-platform-v1.png`、`art/prompt-apron-platform.txt`，JPEG成品315,353字节。透明素材继续沿用先生成纯绿背景再抠图的既有成果。

最终交互：去掉候机大厅工具行，待运与已装机客货在同一目的地组内原位装卸；左侧独立路牌支持同站装机，隐藏滚动条但保留触摸、箭头和键盘滚动。“制定路线”保持右下角导航位置，飞行时隐藏整个客货区、将锁定收支放到飞行场景，抵达后恢复。首航任务引导及前批按钮修复继续保留。

## 本地验证

- 类型检查通过。
- 294项核心/存档单元测试通过。
- npm audit全部依赖审计：0漏洞。
- 根路径生产构建通过；最终完整96项浏览器回归通过，0重试。
- Pages子路径生产构建通过；最终完整96项浏览器回归通过，0重试。
- 两种构建各预缓存45项，全部28项美术与源文件SHA256一致；`git diff --check`通过。

中间版完整回归89通过、7失败：一项把卸载后的“中转保留”误断言为普通待运；三项仍假设补能时已装机订单不显示；三项使用已移除工具行定位收支。已按真实业务语义更新断言，并在飞行时移除客货区后验证飞行隐藏、刷新不恢复、抵达后恢复；未放宽核心规则或导入校验。

覆盖1440×900、844×390、667×375、竖屏、装卸原位稳定、左右翻页与Home/End、多目的地各自左侧路牌、任务首航、能量/中转/自动经营、刷新、导入导出、备份恢复、全部28项美术离线解码。已检查手机横屏站牌、客货背景与导航画面。截图由完整回归保存在 `artifacts/`。

源码及根/子路径静态包使用 `-v5.zip` 命名，旧版交付保留。构建仍有既有Pixi包超过500kB提示；真实手机/iOS Safari尚未验证。提交和部署通过功能分支PR、squash合并及仓库现有Pages工作流执行，线上结果以对应工作流回执和公开页面检查为准。
