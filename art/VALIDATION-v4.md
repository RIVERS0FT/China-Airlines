# 客货站牌与任务引导验收（2026-09-12）

沿用 `feat/airport-art-assets` 当前工作区和此前27项美术，不重新生成图片，不修改核心经营或存档规则。本轮处理以下用户要求：

- 删除“显示客货”开关，机体客货示意始终可见。
- 选择航线按钮完整进入底部导航右下角，客货列表恢复全宽；禁用原因在按钮内显示。
- 删除背景上重复的机场名称和代码，顶部机场信息保留。
- 按目的机场分组共用站牌，删除每单头顶的目的地标签；单体人物与货物仍逐个装卸。
- 删除底部起航引导按钮及其下方说明；任务牌提示真实首航进度，任务内提供装载、规划、查看航班入口，沿用现有奖励领取规则。帮助内已有按需分步引导保留。

## 检查

- 独立类型检查通过；294项核心/存档单元测试通过。
- 根路径、Pages `/China-Airlines/` 生产构建通过，各预缓存44项。
- 布局调整中间版本16项机场/队列/场景专项通过。
- 最终应用根路径完整回归95通过、1失败，失败是新增多目的地测试数据未通过既有严格导入校验；测试最初漏接导入确认，随后修改目的地又未同步运费。已补确认处理、使用 `orderReward` 构造合法运费并在浏览器导入前调用 `validateSave`；未修改校验器。
- 修正测试数据后根路径7项机场专项全部通过，0重试，包含多目的地独立装载、任务引导首航与刷新、桌面和两种手机横屏按钮操作。最终96项完整根路径未重复执行，完整运行报告保存在 `artifacts/browser-root-v4-full.json`，专项报告在 `artifacts/browser-root.json`。
- 最终Pages子路径完整96项回归全部通过，0失败、0跳过、0重试，约2.5分钟。
- `git diff --check`通过；两个构建中的27项美术与源文件SHA256一致。

覆盖1440×900、844×390、667×375、触控、竖屏提示、装卸、机库、地图、真实起飞、刷新、导入导出、损坏存档恢复和离线启动。截图 `artifacts/airport-controls-1440.png`、`airport-controls-844.png`、`airport-controls-667.png`；多目的地装载后截图 `artifacts/destination-stations-loaded.png`。已检查手机横屏画面，按钮在底部，队列占满宽度，客货示意常显。

保留既有Pixi渲染包超过500kB提示。未验证真实手机/iOS Safari，不表示完整复刻。源码、素材、根路径静态和Pages静态ZIP以 `-v4.zip` 命名，保留v1—v3交付物；没有提交、推送、PR或部署。
